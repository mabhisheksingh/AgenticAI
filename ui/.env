# UI environment variables (Vite uses VITE_* at build time)
# Backend server base URL
VITE_API_BASE_URL=http://localhost:8080

# API path prefix (if any)
VITE_API_PATH=/v1

# Request timeout (ms)
VITE_REQUEST_TIMEOUT_MS=1000000